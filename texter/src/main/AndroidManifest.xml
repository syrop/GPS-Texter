<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="pl.org.seva.texter"
    android:versionCode="5"
    android:versionName="1.1.1" >
    
     <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
     <uses-permission android:name="android.permission.SEND_SMS" />
     <uses-permission android:name="android.permission.READ_CONTACTS" />
     <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name="android.support.multidex.MultiDexApplication"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <!-- Remove .release when using in release mode. -->
        <meta-data
        android:name="com.google.android.geo.API_KEY.release"
        android:value=" AIzaSyA6xLUDawVBKqWklVFNFybZPp8zlQMhdoo "/>

        <!-- Change name (e.g. by adding .debug) or delete if using in release mode. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value=" AIzaSyC-CTsb0OYlo93b9_lWKcAaae5q8v3goD8 "/>

        <activity
            android:name="pl.org.seva.texter.activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="pl.org.seva.texter.activities.SettingsActivity"
            android:theme="@style/FixForPreferenceThemeOverlay"
            android:label="@string/app_name_settings"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.HomeLocationActivity"
            android:theme="@style/FixForPreferenceThemeOverlay"
            android:label="@string/app_name_settings"
            android:screenOrientation="portrait" />

        <service android:name=".services.TexterService"/>
        <service android:name=".services.GeofenceTransitionsIntentService"/>


        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>
    </application>

</manifest>
